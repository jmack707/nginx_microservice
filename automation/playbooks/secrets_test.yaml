---
- name: Playbook for test secrets
  hosts: all
  gather_facts: false

- name: Use vault variables
  hosts: databases
  vars_files: k8s_secret.yaml 
  tasks:

  - name: Show vault variable value
    debug:
      var: bigip_password