---
- hosts: localhost
  gather_facts: false
  become: true
  
  vars_files:
    - vars/docker_login.yaml
    - vars/docker_images.yaml

  tasks:
    - name: Login to Repo
      import_role:
        name: docker_login

    - name: Pull Docker Images
      import_role:
        name: docker_pull

  

